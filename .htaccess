# Canonical HTTPS/non-WWW
<IfModule mod_rewrite.c>
	RewriteCond %{HTTPS} off [OR]
	RewriteCond %{HTTP_HOST} ^www\.radiomagma\.com.ar [NC]
	RewriteRule (.*) https://radiomagma.com.ar/$1 [L,R=301]
</IfModule>
